@* 
    @model <type of data it is expecting> ==> used to accept the data passed into the view 
    can be from controller, pagemodel, view or any other page
    but we have to reference the type which can be either value or refernece type
    so whatever value that is coming in from the type referenced above is now stored in the Model variable
*@
@model int

    <td style="width:150px">
        <div class="btn-group" role="group">
            <a class="btn btn-primary text-white" asp-action="Edit" asp-route-Id="@Model">
                <i class="fas fa-edit"></i>
            </a>

            <a class="btn btn-success text-white" asp-action="Details" asp-route-Id="@Model">
                <i class="fas fa-list-alt"></i>
            </a>

            <a class="btn btn-danger text-white" asp-action="Delete" asp-route-Id="@Model">
                <i class="fas fa-trash-alt"></i>
            </a>
        </div>
    </td>
